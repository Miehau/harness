# Agent Plan runner

Follow `.agents/skills/agent-plan/SKILL.md`.

The active application is `runner/`. Read `runner/README.md` for commands and boundaries.
Use `npm test` and `npm run check`; tests use disposable Git repos and mocked agents.
`npm run probe` is an opt-in live Herdr connection check with no model calls.

The former visual pipeline is retired and retained only in `archive/`.
Do not restore it, import its modules, or apply its historical instructions to active work.
