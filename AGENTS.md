# Agent Plan Workspace

Follow `.agents/skills/agent-plan/SKILL.md`.

Run `node scripts/nav.mjs`, `node scripts/test.mjs`, and `node scripts/seed.mjs` instead of reconstructing the app from memory.

Drive the daemon with `node src/cli.js` (`new text`, `list backlog`, `select`, `approve`, `list timeline`, `queue clear`). Same HTTP routes as the dashboard.
